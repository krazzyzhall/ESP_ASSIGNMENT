#KDIR_L:=/usr/src/linux-headers-`uname -r`/build
#PWD:= $(shell pwd)
KDIR_L:=/lib/modules/`uname -r`/build
KDIR:=/home/krazzy/SDK/sysroots/i586-poky-linux/usr/src/kernel
CC= gcc
CC_G = i586-poky-linux-gcc
ARCH = x86
CROSS_COMPILE = i586-poky-linux-
SROOT=/home/krazzy/SDK/sysroots/i586-poky-linux/
#CFLAGS += -c -pthread
APP = Squeue_test

obj-m:= Squeue.o

#i686: Squeue_test
#	make ARCH=x86 -C $(KDIR_L) M=$(PWD) modules
#Squeue_test:
#	$(CC) -pthread -o Squeue_test main.o
#main.o: 
#	$(CC) -c -pthread main.c
#app.o : app.c
#	$(CC) $(CFLAGS) app.c

all: Squeue_test
	make ARCH=x86 CROSS_COMPILE=i586-poky-linux- -C $(KDIR) M=$(PWD) modules
Squeue_test:
	i586-poky-linux-gcc -pthread -o Squeue_test main.c --sysroot=$(SROOT)
clean:
	rm -f *.ko
	rm -f *.o
	rm -f Module.symvers
	rm -f modules.order
	rm -f *.mod.c
	rm -rf .tmp_versions
	rm -f *.mod.c
	rm -f *.mod.o
	rm -f \.*.cmd
	rm -f Module.markers
	rm -f $(APP) 
